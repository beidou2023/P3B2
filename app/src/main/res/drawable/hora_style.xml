<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent"/> <!-- Fondo transparente -->
    <stroke android:width="1dp" android:color="@android:color/black"/> <!-- Borde inferior -->
    <padding android:top="1dp" android:bottom="1dp"/> <!-- Espaciado para el borde superior -->
    <size android:height="1dp"/> <!-- Borde superior -->
</shape>